version: "3.9"
services:
  python311:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: raspios-lite-armhf-cp311
    platform: linux/arm/v7
    tty: true
    volumes:
      - .:/workspace
    working_dir: /workspace
